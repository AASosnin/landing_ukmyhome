// Как запустить УК. Открывающийся список
howToMakeUKTemplate(howToMakeUKList);

// Что такое франшиза. Список
whatIsFranchiseTemplate(whatIsFranchiseList);

// Разниза УК и ТСЖ. Карточки
bodyCardTemplate(bodyCardList);

// Сколько стоит франшиза. Список. Блоки с ценой.
pricePageListTemplate(pricePageListList);
pricePageBlocksTemplate(pricePageBlocksList);

// Дополнительные услуги. Иконки-кнопки и открывающиеся блоки с текстом
dopPageTemplate(dopPageList);

// Контакты. Левый блок с контактами
contactsTemplate(contactsList);

// Кнопка в правом нижнем углу экрана
createWindowFormBtn('Заказать звонок');

// const mod = myModal;